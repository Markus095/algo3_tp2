package edu.fiuba.algo3.modelo;
/* 
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
public class TestVehiculo {
    @Test
    public void unaMotoAtraviesaElMapaYLlegaAlDestino(){
        Posicion posicionVehiculo = new posicionTransitable(0,0);
        Moto moto = new Moto();

        Posicion posicionDestino = new Posicion(1,1);

        Mapa mapa = new Mapa(posicionDestino);

        mapa.posicionar(moto, posicionVehiculo);

        Direccion derecha = new Direccion('D');
        Direccion abajo = new Direccion('S');

        Posicion posVehiculoSiguiente = mapa.mover(moto, posicionVehiculo, derecha);
        Posicion posFinal = mapa.mover(moto, posVehiculoSiguiente, abajo);

        assertEquals(mapa.gano(), true);
    }
}
 */